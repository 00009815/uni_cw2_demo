extends layout.pug

block content
  main.container
    h1.page-title All notes

    .notes
      each note in n 
        div.note-wrapper
          a.note(href=`/notes/${note.id}`)= note.title 
          a.delete(href=`/notes/${note.id}/delete`) Delete